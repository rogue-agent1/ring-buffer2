# ring_buffer2

Fixed-size ring buffer (circular buffer) with overflow handling

Single-file, zero-dependency Python CLI tool.

## Usage

```bash
python ring_buffer2.py --help
```

## Test

```bash
python -c "import ring_buffer2; ring_buffer2.test()"
```
